{
  "name": "example",
  "type": "module",
  "description": "Fastify Edge example",
  "devDependencies": {
    "esbuild":"^0.14.38",
    "miniflare":"^2.4.0"
  },
  "dependencies": {
     "fastify-edge": "^0.0.4"
  },
  "main":"./dist/index.js",
  "scripts":{
     "build":"esbuild --bundle --sourcemap --outdir=dist ./index.js",
     "dev":"miniflare --watch --debug"
  }
}